{
    "outputDirectory": "dist",
    "installCommand": "npm install",
    "buildCommand": "git fetch --depth=2147483647 origin && git rev-list --count HEAD && npm run build",  
    "devCommand": "npm run start",
    "routes": [
        { "handle": "filesystem" },
        { "src": "/(.*)", "status": 404, "dest": "/404" }
    ]
}